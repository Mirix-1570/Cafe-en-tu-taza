export class Farm {
  constructor({ id, nombre, ubicacion, extension, producerId }) {
    this.id = id;
    this.nombre = nombre;
    this.ubicacion = ubicacion;
    this.extension = extension;
    this.producerId = producerId;
  }
}
